SECRET_KEY=change-me-to-a-random-secret
ADMIN_USER=admin
ADMIN_PASS=admin123

DB_BACKEND=filesystem
FS_DATA_PATH=/Users/alexandregans/sandbox/data/web-script

# Storage backend: "postgres" or "filesystem"
#DB_BACKEND=postgres

# PostgreSQL (only used when DB_BACKEND=postgres)
# DATABASE_URL is set automatically by docker-compose when using the db service

# Filesystem (only used when DB_BACKEND=filesystem)
# FS_DATA_PATH=/app/data
